sudo: required

language: java

services:
- docker

before_install:
- docker pull clockercentral/clocker
- sudo service docker restart ; sleep 10
- docker run -d -P clockercentral/clocker
#- docker run -d -v ~/.brooklyn:/root/.brooklyn -v ~/.ssh:/root/.ssh -P clockercentral/clocker

script:
- ls -al
